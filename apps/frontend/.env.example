VITE_PORT=5173
VITE_BACKEND_PROXY_TARGET=http://localhost:3001

# Optional: set this only if you want the frontend to call the backend directly
# instead of using the Vite dev proxy.
# VITE_API_BASE_URL=http://localhost:3001
